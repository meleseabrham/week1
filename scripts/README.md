# Scripts

Drop CLI-ready utilities here, e.g. data ingestion, preprocessing, and reporting jobs. Prefer keeping reusable logic in `src/` and make scripts thin wrappers that parse arguments and orchestrate workflows.
